language: cpp
compiler:
    - gcc
    - clang
before_install:
    - git submodule update --init --recursive
before_script:
    - mkdir build && cd build && cmake .. -DCMAKE_BUILD_TYPE=Debug
script:
    - make
