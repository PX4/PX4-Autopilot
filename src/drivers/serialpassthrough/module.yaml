module_name: Serial Passthrough (MAVLink)

parameters:
    - group: Serial Passthrough
      definitions:
        PASSTHRU_EN:
            description:
                short: Serial passthrough enable
                long: Enables serial passthrough; dshot and pwm_out are not started at boot.
            type: boolean
            default: 0
            reboot_required: true
