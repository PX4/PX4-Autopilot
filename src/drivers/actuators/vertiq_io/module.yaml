module_name: Vertiq IO
serial_config:
    - command: vertiq_io telemetry ${SERIAL_DEV}
      port_config_param:
        name: VERTIQ_IO_CFG
        group: Vertiq IO

actuator_output:
  output_groups:
    - param_prefix: VERTIQ_IO
      group_label: 'ESCs'
      channel_label: 'ESC'
      standard_params:
        min: { min: 0, max: 8191, default: 1 }
        max: { min: 0, max: 8191, default: 8191 }
        failsafe: { min: 0, max: 8191 }
      num_channels: 8

parameters:
    - group: Vertiq IO
      definitions:
        VERTIQ_TEST:
            description:
                short: testing to see if I can get this to show
                long: |
                    I just want this stupid thing to stupid show up!!! grrrrr
            type: int32
            default: 26
        VERTIQ_ENABLE:
            description:
                short: Enable the IQUART driver
                long: |
                    Determine whether or not to enable the IQUART driver. If this value is greater than 0, the driver is enabled.
            type: int32
            default: 1
        VERTIQ_BAUD:
            description:
                short: The IQUART driver's baud rate
                long: |
                    The baud rate (in bits per second) used by the serial port connected with IQUART communication
            type: int32
            default: 115200
