module_name: Sagetech MXS
serial_config:
    # - command: sagetech_mxs start -d ${SERIAL_DEV} -b p:${BAUD_PARAM}
    - command: sagetech_mxs start -d ${SERIAL_DEV}
      port_config_param:
        name: MXS_SER_PORT
        group: Transponder
        default: TEL2
      label: Sagetech MXS Serial Port
