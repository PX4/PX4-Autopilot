__max_num_config_instances: &max_num_config_instances 1

module_name: TMP102

parameters:
  - group: Sensors
    definitions:
      SENS_EN_TMP102:
        description:
            short: Enable TMP102
        type: boolean
        reboot_required: true
        default: 0
