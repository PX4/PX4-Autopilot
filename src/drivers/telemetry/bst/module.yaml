module_name: bst
parameters:
  - definitions:
      TEL_BST_EN:
        default: 0
        description:
          long: If true, the FMU will try to connect to Blacksheep telemetry on start up
          short: Blacksheep telemetry Enable
        reboot_required: true
        type: boolean
        volatile: false
    group: Telemetry
