module_name: Ultrawideband position sensor driver
serial_config:
  - command: uwb start -d ${SERIAL_DEV} -b p:${BAUD_PARAM}
    port_config_param:
      name: UWB_CONFIG
      group: UWB
      default: TEL2
