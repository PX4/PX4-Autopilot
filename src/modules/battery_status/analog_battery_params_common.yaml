module_name: battery_status
parameters:
- group: Battery Calibration
  definitions:
    BAT_V_OFFS_CURR:
      description:
        short: Offset in volt as seen by the ADC input of the current sensor
        long: Subtracted before computing battery current from voltage.
      type: float
      default: 0.0
      decimal: 8
