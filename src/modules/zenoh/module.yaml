module_name: Zenoh bridge

parameters:
    - group: Zenoh
      definitions:

        ZENOH_ENABLE:
            description:
                short: Enable Zenoh
                long: "Starts the Zenoh-Pico Node driver. See https://docs.px4.io/main/en/middleware/zenoh"
            category: System
            type: boolean
            reboot_required: true
            default: 0
