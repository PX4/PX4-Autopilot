menuconfig MODULES_FW_AUTOTUNE_ATTITUDE_CONTROL
	bool "fw_autotune_attitude_control"
	default y if TARGET_CLASS_PLANE || TARGET_CLASS_VTOL || TARGET_CLASS_LINUX
	default n
	---help---
		Enable support for fw_autotune_attitude_control
