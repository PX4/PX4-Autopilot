#!/usr/bin/bash

# PX4 commands need the 'px4-' prefix in bash.
source alias.sh

uorb start

param load
param set SYS_RESTART_TYPE 0

dataman start

rgbledsim start
tone_alarm start

ver all

tests all

shutdown
