#!/bin/sh
#
# PX4 FMUv4 specific board extras init
#------------------------------------------------------------------------------

# Run FrSky Telemetry on the dedicated FrSky port if FrSky is not already a SER_* protocol.
if param compare -s SER_TEL1_PROTO 21
then
elif param compare -s SER_TEL2_PROTO 21
then
elif param compare -s SER_WIFI_PROTO 21
then
else
	frsky_telemetry start -d /dev/ttyS6 -t 15
fi
